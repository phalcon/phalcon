# This file is part of Phalcon.
#
# (c) Phalcon Team <team@phalcon.io>
#
# For the full copyright and license information, please view
# the LICENSE file that was distributed with this source code.

name: Phalcon CI

on:
  schedule:
    - cron: '0 2 * * *' # Daily at 02:00 runs only on default branch
  push:
    paths-ignore:
      - '**.md'
      - '**.txt'
  pull_request:
  workflow_dispatch:

env:
  fail-fast: true

  # All versions should be declared here
  PHALCON_VERSION: 6.0.0
  CODACY_PROJECT_TOKEN: ${{ secrets.CODACY_PROJECT_TOKEN }}

  # For tests
  LANG: en_US.UTF-8
  LANGUAGE: en_US.UTF-8
  LC_ALL: en_US.UTF-8

  # Windows specific
  TOOLS_DIR: 'C:\tools'

  # PHP extensions required by Composer
  EXTENSIONS: apcu, gettext, gd, igbinary, imagick, intl, json, mbstring, msgpack, memcached, sqlite3, yaml, redis

permissions: {}
jobs:

  # PHP CodeSniffer inspection
  phpcs:
    permissions:
      contents: read

    name: Validate Tests code style
    if: "!contains(github.event.head_commit.message, 'ci skip')"

    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3

      - name: "Setup PHP"
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.0
          extensions: ${{ env.EXTENSIONS }}
          ini-values: apc.enable_cli=on, session.save_path=/tmp
          tools: pecl
        env:
          COMPOSER_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: "Install development dependencies with Composer"
        uses: "ramsey/composer-install@v2"
        with:
          composer-options: "--prefer-dist"

      - name: "PHPCS"
        run: |
          vendor/bin/phpcs --standard=./phpcs.xml


  unit-tests:
    needs: phpcs

    permissions:
      contents: read # to fetch code (actions/checkout)

    name: unit tests / PHP-${{ matrix.php }}-${{ matrix.ts }}-${{ matrix.name }}
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        php:
          - '8.0'
          - '8.1'
        ts:
          - 'nts'
          - 'ts'
        name:
          - ubuntu-gcc
          - macos-clang
        # matrix names should be in next format:
        #     {php}-{ts}-{os.name}-{compiler}-{arch}
        include:
          # Linux
          - { name: ubuntu-gcc, os: ubuntu-20.04, compiler: gcc }
#          # macOS
          - { name: macos-clang, os: macos-11, compiler: clang }
          # Windows
          - { php: '8.0', ts: 'ts',  arch: 'x64', name: 'windows2019-vs16', os: 'windows-2019', compiler: 'vs16' }
          - { php: '8.0', ts: 'nts', arch: 'x64', name: 'windows2019-vs16', os: 'windows-2019', compiler: 'vs16' }
          - { php: '8.1', ts: 'ts',  arch: 'x64', name: 'windows2019-vs16', os: 'windows-2019', compiler: 'vs16' }
          - { php: '8.1', ts: 'nts', arch: 'x64', name: 'windows2019-vs16', os: 'windows-2019', compiler: 'vs16' }

    steps:
      - uses: actions/checkout@v3
      - name: "Setup platform specific environment"
        shell: pwsh
        run: |
          git config --global core.autocrlf false
          $SessionSavePath = if ("${{ runner.os }}" -eq "Windows") { 'C:\temp' } else { '/tmp' }
          Write-Output "SESSION_SAVE_PATH=$SessionSavePath" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: "Setup PHP"
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: ${{ env.EXTENSIONS }}
          ini-values: apc.enable_cli=on, session.save_path=${{ env.SESSION_SAVE_PATH }}
          tools: pecl, phpize, php-config, composer:v2
          coverage: pcov
        env:
          PHPTS: ${{ matrix.ts }}
          COMPOSER_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: "Validate composer"
        run: composer validate --no-check-all --no-check-publish

      - name: "Install development dependencies with Composer"
        uses: "ramsey/composer-install@v2"
        with:
          composer-options: "--prefer-dist"

      - name: "Setup Tests"
        shell: bash
        run: |
          if [ "${{ runner.os }}" = "Linux" ]; then
            ./config/ci/linux-setup-locales.sh
          fi

          cp tests/_config/.env.default .env
          vendor/bin/codecept build

      - name: "Run Unit Tests"
        if: always()
        run: |
          vendor/bin/codecept run --coverage-xml=coverage.xml --ext DotReporter unit

      - name: "Upload coverage file artifact"
        uses: "actions/upload-artifact@v3"
        with:
          name: "unit-${{ matrix.php }}-${{ matrix.ts }}-${{ matrix.name }}.coverage"
          path: "tests/_output/coverage.xml"


  cli-tests:
    needs: phpcs

    permissions:
      contents: read # to fetch code (actions/checkout)

    name: cli tests / PHP-${{ matrix.php }}-${{ matrix.ts }}-${{ matrix.name }}
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        php:
          - '8.0'
          - '8.1'
        ts:
          - 'nts'
          - 'ts'
        name:
          - ubuntu-gcc
          - macos-clang
        # matrix names should be in next format:
        #     {php}-{ts}-{os.name}-{compiler}-{arch}
        include:
          # Linux
          - { name: ubuntu-gcc, os: ubuntu-20.04, compiler: gcc }
          # macOS
          - { name: macos-clang, os: macos-11, compiler: clang }
          # Windows
          - { php: '8.0', ts: 'ts',  arch: 'x64', name: 'windows2019-vs16', os: 'windows-2019', compiler: 'vs16' }
          - { php: '8.0', ts: 'nts', arch: 'x64', name: 'windows2019-vs16', os: 'windows-2019', compiler: 'vs16' }
          - { php: '8.1', ts: 'ts',  arch: 'x64', name: 'windows2019-vs16', os: 'windows-2019', compiler: 'vs16' }
          - { php: '8.1', ts: 'nts', arch: 'x64', name: 'windows2019-vs16', os: 'windows-2019', compiler: 'vs16' }

    steps:
      - uses: actions/checkout@v3
      - name: "Setup platform specific environment"
        shell: pwsh
        run: |
          git config --global core.autocrlf false
          $SessionSavePath = if ("${{ runner.os }}" -eq "Windows") { 'C:\temp' } else { '/tmp' }
          Write-Output "SESSION_SAVE_PATH=$SessionSavePath" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: "Setup PHP"
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: ${{ env.EXTENSIONS }}
          ini-values: apc.enable_cli=on, session.save_path=${{ env.SESSION_SAVE_PATH }}
          tools: pecl, phpize, php-config, composer:v2
          coverage: pcov
        env:
          PHPTS: ${{ matrix.ts }}
          COMPOSER_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: "Validate composer"
        run: composer validate --no-check-all --no-check-publish

      - name: "Install development dependencies with Composer"
        uses: "ramsey/composer-install@v2"
        with:
          composer-options: "--prefer-dist"

      - name: "Setup Tests"
        shell: bash
        run: |
          if [ "${{ runner.os }}" = "Linux" ]; then
            ./config/ci/linux-setup-locales.sh
          fi

          cp tests/_config/.env.default .env
          vendor/bin/codecept build

      - name: "Run CLI Tests"
        if: always()
        run: |
          vendor/bin/codecept run --coverage-xml=coverage.xml --ext DotReporter cli

      - name: "Upload coverage file artifact"
        uses: "actions/upload-artifact@v3"
        with:
          name: "unit-${{ matrix.php }}-${{ matrix.ts }}-${{ matrix.name }}.coverage"
          path: "tests/_output/coverage.xml"


  integration-tests:
    needs: phpcs

    permissions:
      contents: read # to fetch code (actions/checkout)

    name: integration tests / PHP-${{ matrix.php }}-${{ matrix.ts }}-${{ matrix.name }}
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        php-version:
          - '8.0'
          - '8.1'
        ts:
          - 'nts'
          - 'ts'
        name:
          - ubuntu-gcc
          - macos-clang
        # matrix names should be in next format:
        #     {php}-{ts}-{os.name}-{compiler}-{arch}
        include:
          # Linux
          - { name: ubuntu-gcc, os: ubuntu-20.04, compiler: gcc }
          # macOS
          - { name: macos-clang, os: macos-11, compiler: clang }
          # Windows
          - { php: '8.0', ts: 'ts',  arch: 'x64', name: 'windows2019-vs16', os: 'windows-2019', compiler: 'vs16' }
          - { php: '8.0', ts: 'nts', arch: 'x64', name: 'windows2019-vs16', os: 'windows-2019', compiler: 'vs16' }
          - { php: '8.1', ts: 'ts',  arch: 'x64', name: 'windows2019-vs16', os: 'windows-2019', compiler: 'vs16' }
          - { php: '8.1', ts: 'nts', arch: 'x64', name: 'windows2019-vs16', os: 'windows-2019', compiler: 'vs16' }

    steps:
      - uses: actions/checkout@v3
      - name: "Setup platform specific environment"
        shell: pwsh
        run: |
          git config --global core.autocrlf false
          $SessionSavePath = if ("${{ runner.os }}" -eq "Windows") { 'C:\temp' } else { '/tmp' }
          Write-Output "SESSION_SAVE_PATH=$SessionSavePath" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: "Setup PHP"
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: ${{ env.EXTENSIONS }}
          ini-values: apc.enable_cli=on, session.save_path=${{ env.SESSION_SAVE_PATH }}
          tools: pecl, phpize, php-config, composer:v2
          coverage: pcov
        env:
          PHPTS: ${{ matrix.ts }}
          COMPOSER_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: "Validate composer"
        run: composer validate --no-check-all --no-check-publish

      - name: "Install development dependencies with Composer"
        uses: "ramsey/composer-install@v2"
        with:
          composer-options: "--prefer-dist"

      - name: "Setup Tests"
        shell: bash
        run: |
          if [ "${{ runner.os }}" = "Linux" ]; then
            ./config/ci/linux-setup-locales.sh
          fi

          cp tests/_config/.env.default .env
          vendor/bin/codecept build

      - name: "Run Integration Tests"
        env:
          DATA_REDIS_PORT: ${{ job.services.redis.ports['6379'] }}
          DATA_MEMCACHED_PORT: ${{ job.services.memcached.ports['11211'] }}
        if: always()
        run: |
          vendor/bin/codecept run --coverage-xml=coverage.xml --ext DotReporter integration

      - name: "Upload coverage file artifact"
        uses: "actions/upload-artifact@v3"
        with:
          name: "unit-${{ matrix.php }}-${{ matrix.ts }}-${{ matrix.name }}.coverage"
          path: "tests/_output/coverage.xml"

#  db-common-tests:
#    needs: phpcs
#
#    permissions:
#      contents: read # to fetch code (actions/checkout)
#
#    name: db common tests / PHP-${{ matrix.php }}
#    runs-on: ubuntu-20.04
#
#    strategy:
#      matrix:
#        php-version:
#          - '8.0'
#          - '8.1'
#
#    services:
#      mysql:
#        image: mysql:5.7
#        ports:
#          - "3306:3306"
#        env:
#          MYSQL_ROOT_PASSWORD: secret
#          MYSQL_USER: phalcon
#          MYSQL_DATABASE: phalcon
#          MYSQL_PASSWORD: secret
#      postgres:
#        image: postgres:12-alpine
#        ports:
#          - "5432:5432"
#        env:
#          POSTGRES_USER: phalcon
#          POSTGRES_PASSWORD: secret
#          POSTGRES_DB: phalcon
#        options: >-
#          --health-cmd pg_isready
#          --health-interval 10s
#          --health-timeout 5s
#          --health-retries 5
#      redis:
#        image: redis:5-alpine
#        ports:
#          - "6379:6379"
#      memcached:
#        image: memcached:1.5-alpine
#        ports:
#          - "11211:11211"
#
#    steps:
#      - uses: actions/checkout@v3
#      - name: Setup platform specific environment
#        shell: pwsh
#        run: |
#          git config --global core.autocrlf false
#
#      - name: Setup PHP
#        uses: shivammathur/setup-php@v2
#        with:
#          php-version: ${{ matrix.php }}
#          extensions: ${{ env.EXTENSIONS }}
#          ini-values: apc.enable_cli=on, session.save_path=${{ env.SESSION_SAVE_PATH }}
#          tools: pecl, phpize, php-config, composer:v2
#          coverage: pcov
#        env:
#          COMPOSER_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#
#      - name: Validate composer
#        run: composer validate --no-check-all --no-check-publish
#
#      - name: Get Composer Cache Directory
#        id: composer-cache
#        run: |
#          echo "::set-output name=dir::$(composer config cache-files-dir)"
#
#      - name: Setup Composer Cache
#        uses: actions/cache@v3
#        with:
#          path: ${{ steps.composer-cache.outputs.dir }}
#          key: ubuntu-composer-${{ matrix.php }}-${{ hashFiles('**/composer.lock') }}
#          restore-keys: ubuntu-composer-${{ matrix.php }}-
#
#      - run: composer install --prefer-dist
#
#      - name: Setup Tests
#        shell: bash
#        run: |
#          if [ "${{ runner.os }}" = "Linux" ]; then
#            ./config/ci/linux-setup-locales.sh
#          fi
#
#          cp tests/_config/.env.default .env
#          vendor/bin/codecept build
#
#      - name: Run Database Tests (Common)
#        env:
#          DATA_MYSQL_PORT: ${{ job.services.mysql.ports['3306'] }}
#          DATA_MYSQL_USER: root
#          DATA_POSTGRES_USER: postgres
#          DATA_POSTGRES_PORT: ${{ job.services.postgres.ports['5432'] }}
#          DATA_REDIS_PORT: ${{ job.services.redis.ports['6379'] }}
#          DATA_MEMCACHED_PORT: ${{ job.services.memcached.ports['11211'] }}
#        run: |
#          vendor/bin/codecept run --coverage-xml=coverage.xml --ext DotReporter database -g common
#
#      - name: "Upload coverage file"
#        uses: "actions/upload-artifact@v3"
#        with:
#          name: "db-mysql-${{ matrix.php }}-${{ matrix.ts }}-${{ matrix.name }}.coverage"
#          path: "coverage.xml"
#
#
#  db-mysql-tests:
#    needs: phpcs
#
#    permissions:
#      contents: read # to fetch code (actions/checkout)
#
#    name: db mysql tests / PHP-${{ matrix.php }}-${{ matrix.ts }}-${{ matrix.name }}
#    runs-on: ubuntu-20.04
#
#    strategy:
#      matrix:
#        php-version:
#          - '8.0'
#          - '8.1'
#
#    services:
#      mysql:
#        image: mysql:5.7
#        ports:
#          - "3306:3306"
#        env:
#          MYSQL_ROOT_PASSWORD: secret
#          MYSQL_USER: phalcon
#          MYSQL_DATABASE: phalcon
#          MYSQL_PASSWORD: secret
#      postgres:
#        image: postgres:12-alpine
#        ports:
#          - "5432:5432"
#        env:
#          POSTGRES_USER: phalcon
#          POSTGRES_PASSWORD: secret
#          POSTGRES_DB: phalcon
#        options: >-
#          --health-cmd pg_isready
#          --health-interval 10s
#          --health-timeout 5s
#          --health-retries 5
#      redis:
#        image: redis:5-alpine
#        ports:
#          - "6379:6379"
#      memcached:
#        image: memcached:1.5-alpine
#        ports:
#          - "11211:11211"
#
#    steps:
#      - uses: actions/checkout@v3
#      - name: Setup platform specific environment
#        shell: pwsh
#        run: |
#          git config --global core.autocrlf false
#
#      - name: Setup PHP
#        uses: shivammathur/setup-php@v2
#        with:
#          php-version: ${{ matrix.php }}
#          extensions: ${{ env.EXTENSIONS }}
#          ini-values: apc.enable_cli=on, session.save_path=${{ env.SESSION_SAVE_PATH }}
#          tools: pecl, phpize, php-config, composer:v2
#          coverage: pcov
#        env:
#          COMPOSER_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#
#      - name: Validate composer
#        run: composer validate --no-check-all --no-check-publish
#
#      - name: Get Composer Cache Directory
#        id: composer-cache
#        run: |
#          echo "::set-output name=dir::$(composer config cache-files-dir)"
#
#      - name: Setup Composer Cache
#        uses: actions/cache@v3
#        with:
#          path: ${{ steps.composer-cache.outputs.dir }}
#          key: ubuntu-composer-${{ matrix.php }}-${{ hashFiles('**/composer.lock') }}
#          restore-keys: ubuntu-composer-${{ matrix.php }}-
#
#      - run: composer install --prefer-dist
#
#      - name: Setup Tests
#        shell: bash
#        run: |
#          if [ "${{ runner.os }}" = "Linux" ]; then
#            ./config/ci/linux-setup-locales.sh
#          fi
#
#          cp tests/_config/.env.default .env
#          vendor/bin/codecept build
#
#      - name: Run Database (MySQL) Tests
#        env:
#          DATA_MYSQL_PORT: ${{ job.services.mysql.ports['3306'] }}
#          DATA_MYSQL_USER: root
#          DATA_REDIS_PORT: ${{ job.services.redis.ports['6379'] }}
#          DATA_MEMCACHED_PORT: ${{ job.services.memcached.ports['11211'] }}
#        run: |
#          vendor/bin/codecept run --coverage-xml=coverage.xml --ext DotReporter database --env mysql -g mysql
#
#      - name: "Upload coverage file"
#        uses: "actions/upload-artifact@v3"
#        with:
#          name: "db-mysql-${{ matrix.php }}-${{ matrix.ts }}-${{ matrix.name }}.coverage"
#          path: "coverage.xml"
#
#
#  db-postgres-tests:
#    needs: phpcs
#
#    permissions:
#      contents: read # to fetch code (actions/checkout)
#
#    name: db postgres tests / PHP-${{ matrix.php }}-${{ matrix.ts }}-${{ matrix.name }}
#    runs-on: ubuntu-20.04
#
#    strategy:
#      matrix:
#        php-version:
#          - '8.0'
#          - '8.1'
#
#    services:
#      mysql:
#        image: mysql:5.7
#        ports:
#          - "3306:3306"
#        env:
#          MYSQL_ROOT_PASSWORD: secret
#          MYSQL_USER: phalcon
#          MYSQL_DATABASE: phalcon
#          MYSQL_PASSWORD: secret
#      postgres:
#        image: postgres:12-alpine
#        ports:
#          - "5432:5432"
#        env:
#          POSTGRES_USER: phalcon
#          POSTGRES_PASSWORD: secret
#          POSTGRES_DB: phalcon
#        options: >-
#          --health-cmd pg_isready
#          --health-interval 10s
#          --health-timeout 5s
#          --health-retries 5
#      redis:
#        image: redis:5-alpine
#        ports:
#          - "6379:6379"
#      memcached:
#        image: memcached:1.5-alpine
#        ports:
#          - "11211:11211"
#
#    steps:
#      - uses: actions/checkout@v3
#      - name: Setup platform specific environment
#        shell: pwsh
#        run: |
#          git config --global core.autocrlf false
#
#      - name: Setup PHP
#        uses: shivammathur/setup-php@v2
#        with:
#          php-version: ${{ matrix.php }}
#          extensions: ${{ env.EXTENSIONS }}
#          ini-values: apc.enable_cli=on, session.save_path=${{ env.SESSION_SAVE_PATH }}
#          tools: pecl, phpize, php-config, composer:v2
#          coverage: pcov
#        env:
#          COMPOSER_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#
#      - name: Validate composer
#        run: composer validate --no-check-all --no-check-publish
#
#      - name: Get Composer Cache Directory
#        id: composer-cache
#        run: |
#          echo "::set-output name=dir::$(composer config cache-files-dir)"
#
#      - name: Setup Composer Cache
#        uses: actions/cache@v3
#        with:
#          path: ${{ steps.composer-cache.outputs.dir }}
#          key: ubuntu-composer-${{ matrix.php }}-${{ hashFiles('**/composer.lock') }}
#          restore-keys: ubuntu-composer-${{ matrix.php }}-
#
#      - run: composer install --prefer-dist
#
#      - name: Setup Tests
#        shell: bash
#        run: |
#          if [ "${{ runner.os }}" = "Linux" ]; then
#            ./config/ci/linux-setup-locales.sh
#          fi
#
#          cp tests/_config/.env.default .env
#          vendor/bin/codecept build
#
#      - name: Run Database (Postgresql) Tests
#        env:
#          DATA_MYSQL_PORT: ${{ job.services.mysql.ports['3306'] }}
#          DATA_MYSQL_USER: root
#          DATA_REDIS_PORT: ${{ job.services.redis.ports['6379'] }}
#          DATA_MEMCACHED_PORT: ${{ job.services.memcached.ports['11211'] }}
#        run: |
#          vendor/bin/codecept run --coverage-xml=coverage.xml --ext DotReporter database --env pgsql -g pgsql
#
#      - name: "Upload coverage file"
#        uses: "actions/upload-artifact@v3"
#        with:
#          name: "db-postgres-${{ matrix.php }}-${{ matrix.ts }}-${{ matrix.name }}.coverage"
#          path: "coverage.xml"
#
#
#  db-sqlite-tests:
#    needs: phpcs
#
#    permissions:
#      contents: read # to fetch code (actions/checkout)
#
#    name: db sqlite tests / PHP-${{ matrix.php }}-${{ matrix.ts }}-${{ matrix.name }}
#    runs-on: ubuntu-20.04
#
#    strategy:
#      matrix:
#        php-version:
#          - '8.0'
#          - '8.1'
#
#    steps:
#      - uses: actions/checkout@v3
#      - name: Setup platform specific environment
#        shell: pwsh
#        run: |
#          git config --global core.autocrlf false
#
#      - name: Setup PHP
#        uses: shivammathur/setup-php@v2
#        with:
#          php-version: ${{ matrix.php }}
#          extensions: ${{ env.EXTENSIONS }}
#          ini-values: apc.enable_cli=on, session.save_path=${{ env.SESSION_SAVE_PATH }}
#          tools: pecl, phpize, php-config, composer:v2
#          coverage: pcov
#        env:
#          COMPOSER_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#
#      - name: Validate composer
#        run: composer validate --no-check-all --no-check-publish
#
#      - name: Get Composer Cache Directory
#        id: composer-cache
#        run: |
#          echo "::set-output name=dir::$(composer config cache-files-dir)"
#
#      - name: Setup Composer Cache
#        uses: actions/cache@v3
#        with:
#          path: ${{ steps.composer-cache.outputs.dir }}
#          key: ubuntu-composer-${{ matrix.php }}-${{ hashFiles('**/composer.lock') }}
#          restore-keys: ubuntu-composer-${{ matrix.php }}-
#
#      - run: composer install --prefer-dist
#
#      - name: Setup Tests
#        shell: bash
#        run: |
#          if [ "${{ runner.os }}" = "Linux" ]; then
#            ./config/ci/linux-setup-locales.sh
#          fi
#
#          cp tests/_config/.env.default .env
#          vendor/bin/codecept build
#
#      - name: Run Database (Sqlite) Tests
#        run: |
#          vendor/bin/codecept run --coverage-xml=coverage.xml --ext DotReporter database --env sqlite -g sqlite
#
#      - name: "Upload coverage file"
#        uses: "actions/upload-artifact@v3"
#        with:
#          name: "db-sqlite-${{ matrix.php }}-${{ matrix.ts }}-${{ matrix.name }}.coverage"
#          path: "coverage.xml"


  upload-coverage:
    permissions:
      contents: read

    name: "Upload coverage to Codecov"
    runs-on: "ubuntu-22.04"
    needs:
      - "unit-tests"
      - "cli-tests"
#      - "integration-tests"
#      - "db-common-tests"
#      - "db-mysql-tests"
#      - "db-postgres-tests"
#      - "db-sqlite-tests"

    steps:
      - name: "Checkout"
        uses: "actions/checkout@v3"
        with:
          fetch-depth: 2

      - name: "Display structure of downloaded files"
        run: |
          mkdir -p reports

      - name: "Download coverage files"
        uses: "actions/download-artifact@v3"
        with:
          path: "reports"

      - name: "Display structure of downloaded files"
        run: ls -R
        working-directory: reports

      - name: "Upload to Codecov"
        uses: "codecov/codecov-action@v3"
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          directory: reports
          fail_ci_if_error: true
          verbose: true
